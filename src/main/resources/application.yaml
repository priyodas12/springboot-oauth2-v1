server:
  port: 9090
spring:
  jpa:
    hibernate:
      ddl-auto: update
  show-sql: true
  datasource:
   url: jdbc:mysql://localhost:3306/oauth2db
   username: 
   password:
   driver-class-name: com.mysql.cj.jdbc.Driver
management:
  endpoints:
    web:
      exposure:
        include: '*'